# AXERRA Architecture

How the pieces fit together: process boot, the Express middleware chain, module
internals, and the data layer. Companion to [CONTRACT.md](./CONTRACT.md), which
states the invariants continuous integration (CI) enforces; this file shows the
shape those invariants protect.

Three things about this architecture are commonly assumed wrong. They are called
out where they appear below, and summarised here:

1. The uniform resource locator (URL) convention is **not** `/api/v1/<module>`.
   The version segment sits inside each module aggregator:
   `/api/<module>/v1/<resource>`.
2. Baseline create/read/update/delete (CRUD) routes are **not** policy-checked.
   `rbac()` is absent from the default chain generated by `createRouter`.
3. The schema-per-tenant switch happens in **two** places, not one, and never
   via `SET search_path`.

---

## 1. System context

```mermaid
flowchart LR
  browser["Browser"]

  subgraph client["apps/client — React 18 + Vite"]
    vite["Vite dev server :5173<br/>proxies /api to :3000"]
    spa["main.jsx to App.jsx<br/>pages / hooks / services"]
  end

  subgraph server["apps/server — Express 5"]
    exp["server.js :3000<br/>to src/app.js"]
  end

  shared["packages/shared<br/>@axerra/shared"]

  subgraph pg["PostgreSQL"]
    admin["admin schema<br/>tenants, portal_users,<br/>portal_user_tenants"]
    tn["tenant schemas<br/>one per tenant"]
  end

  redis["Redis — optional<br/>perm canons, impersonation"]

  browser --> vite --> spa
  spa -->|"fetch, credentials: include"| exp
  exp --> admin
  exp --> tn
  exp <-.->|"cache, fails open"| redis
  shared -.-> spa
  shared -.-> exp
```

Redis is an **optimisation, not a dependency**. Every Redis path in
[authRedis.js](../../apps/server/src/middleware/authRedis.js) and
[permissionLoader.js](../../apps/server/src/services/permissionLoader.js) is
wrapped in a silent catch that falls through to Postgres. Two things are stored:
permission canons at `perm:<userId>:<tenantCode>` with a 900-second time to live
(TTL), and impersonation state at `imp:<userId>`.

`@axerra/shared` is deliberately tiny — it exports exactly five symbols
(`resolveLevel`, `TAX_TYPES`, `COUNTRIES`, `formatByPattern`, `stripFormatting`)
and is consumed by both apps through npm workspaces resolution (ADR-0011).

---

## 2. Server boot and the middleware chain

[server.js](../../apps/server/server.js) is 23 lines: load `dotenv/config`,
import `src/app.js`, `app.listen(PORT)`. There is no `http.createServer`, no
SIGTERM handler, and no explicit database or Redis initialisation — the database
pool is a side effect of the first `import db.js`, and Redis connects lazily on
first use.

```mermaid
flowchart TD
  boot["server.js<br/>dotenv, app.listen :3000"] --> load["app.js module load:<br/>registerAuditResolver"]

  load --> cors["cors — credentials: true"]
  cors --> json["express.json"]
  json --> urlenc["express.urlencoded"]
  urlenc --> cookie["cookieParser"]
  cookie --> morgan["morgan dev — request log"]
  morgan --> health["GET /api/health<br/>before auth"]
  health --> auth["use /api — authRedis"]
  auth --> audit["use /api — auditContext<br/>AsyncLocalStorage scope"]
  audit --> routes["use /api — apiRoutes"]
  routes --> root["GET /"]
  root --> nf["404 catch-all"]
  nf --> err["errorHandler"]
```

Absent, contrary to expectation: **no `helmet`, no `express-session`, no
rate-limit middleware, and no app-wide `passport`.** None of the first three is
even a declared dependency. `passport-local` is used only inside
`authController.login`, with `{ session: false }`.

Winston is for application logs; `morgan('dev')` is the request logger.

### 2.1 Inside authRedis

```mermaid
flowchart TD
  in["Request to /api/*"] --> bypass{"Path in bypass list?<br/>/ , /auth/login,<br/>/auth/refresh, /auth/logout"}
  bypass -->|yes| skip["next — no req.user"]
  bypass -->|no| jwt["jwt.verify auth_token cookie"]

  jwt --> hydrate["admin.portal_users lookup<br/>+ earliest active<br/>admin.portal_user_tenants binding"]
  hydrate --> hdr{"x-tenant-code header<br/>differs from home tenant?"}
  hdr -->|no| useHome["Use home tenant"]
  hdr -->|yes| guard["admin.tenants lookup<br/>+ cross-tenant binding guard<br/>403 unless home is ROOT_TENANT_CODE"]

  useHome --> perms
  guard --> perms["GET perm:uid:tenant from Redis"]
  perms -->|hit| stale
  perms -->|miss| loadp["loadPermissions from tenant schema<br/>roles, policies, state filters,<br/>field groups, then primePermCache"]
  loadp --> stale["calcPermHash vs ph claim<br/>mismatch sets X-Token-Stale: 1"]

  stale --> imp["Impersonation overlay<br/>from imp:uid"]
  imp --> out["Populate req.user and req.ctx<br/>ctx.schema, ctx.tenant, ctx.perms"]
```

This is the first of the two places the tenant schema is resolved. It writes the
resolved name to `req.user.schema_name` and `req.ctx.schema`.

---

## 3. Route registry and URL shape

[apiRoutes.js](../../apps/server/src/apiRoutes.js) mounts ten module aggregators
under `/api`. The `system/` and `modules/` split is load-bearing — architecture
lint rules (ADR-0022) treat them differently.

```mermaid
flowchart LR
  api["src/apiRoutes.js<br/>mounted at /api"]

  subgraph sys["src/system — platform"]
    a["auth"]
    t["tenants"]
    c["core"]
  end

  subgraph mods["src/modules — business"]
    p["projects"]
    ac["activities"]
    b["bom"]
    ap["ap"]
    ar["ar"]
    acc["accounting"]
    r["reports"]
  end

  api --> a & t & c
  api --> p & ac & b & ap & ar & acc & r
```

Each aggregator owns its own version segment, so the shape is:

```
/api/<module>/v1/<resource>
```

| Example                        | Module     | Resource     |
| ------------------------------ | ---------- | ------------ |
| `/api/core/v1/vendors`         | core       | vendors      |
| `/api/ar/v1/ar-invoices`       | ar         | ar-invoices  |
| `/api/tenants/v1/portal-users` | tenants    | portal-users |
| `/api/auth/login`              | auth       | — (flat)     |

`/api/auth/*` is the one exception: `authRouter` mounts flat, with no `v1`
segment.

---

## 4. Module anatomy

Every module follows the same layout. Accounts receivable (`ar`) is the worked
example below because it exercises the full set.

```
src/modules/ar/
  apiRoutes/v1/  arApiRoutes.js  arInvoicesRouter.js  ...
  controllers/   arInvoicesController.js
  models/        ArInvoices.js
  schemas/       arInvoicesSchema.js
  schema/migrations/
  arRepositories.js
```

```mermaid
flowchart TD
  agg["arApiRoutes.js<br/>use /v1/ar-invoices"] --> rtr["arInvoicesRouter.js"]

  rtr --> custom["Custom routes declared FIRST<br/>PUT /approve<br/>withMeta to moduleEntitlement<br/>to addAuditFields to rbac full"]
  rtr --> factory["use / — createRouter<br/>controller, extendRoutes, options"]

  custom --> ctl
  factory --> ctl["arInvoicesController<br/>extends BaseController<br/>rbacConfig scopeColumn: project_id<br/>VALID_TRANSITIONS state machine"]

  ctl -->|"cross-module barrel"| svc["accounting/services<br/>postARInvoice<br/>core numberingService allocateNumber"]
  ctl --> model["models/ArInvoices.js<br/>extends pg-schemata TableModel"]
  model --> schema["schemas/arInvoicesSchema.js<br/>dbSchema: tenantid placeholder<br/>columns, constraints, softDelete"]
  schema --> table["SQL against tenant_schema.ar_invoices"]

  base1["lib/ViewController.js<br/>get, getById, getWhere, exportXls<br/>getSchema, handleError<br/>RBAC layers 2-4"] -.->|extends| base2
  base2["lib/BaseController.js<br/>create, update, archive, restore<br/>bulkInsert, bulkUpdate, importXls"] -.->|extends| ctl
```

There is **no validator or data transfer object (DTO) layer.** Validation is the
pg-schemata schema definition, plus ad-hoc controller checks such as
`VALID_TRANSITIONS` and `SCHEMA_NAME_RE`. `zod` is a declared dependency with
zero imports in `src/`.

`services/` appears only where logic spans entities — `numberingService` in
`core`, the posting barrel in `accounting` (ADR-0019). The `reports` module
skips models entirely and issues raw `SELECT * FROM <schema>.vw_*` against SQL
views (ADR-0020).

### 4.1 What createRouter generates

[createRouter.js](../../apps/server/src/lib/createRouter.js) is the route
factory behind nearly every resource. It generates 13 routes, each with
`disable*` opt-outs:

| Method | Path            | Controller method |
| ------ | --------------- | ----------------- |
| POST   | `/`             | `create`          |
| GET    | `/`             | `get`             |
| GET    | `/where`        | `getWhere`        |
| GET    | `/archived`     | `getWhere`        |
| GET    | `/ping`         | — (inline `pong`) |
| GET    | `/:id`          | `getById`         |
| POST   | `/bulk-insert`  | `bulkInsert`      |
| POST   | `/import-xls`   | `importXls`       |
| POST   | `/export-xls`   | `exportXls`       |
| PUT    | `/bulk-update`  | `bulkUpdate`      |
| PUT    | `/update`       | `update`          |
| DELETE | `/archive`      | `archive`         |
| PATCH  | `/restore`      | `restore`         |

Chain assembly, per method:

```mermaid
flowchart LR
  subgraph read["Reads — GET"]
    r1["userMws<br/>incl. withMeta"] --> r2["moduleEntitlement"] --> r3["handler"]
  end

  subgraph write["Mutations — POST PUT DELETE PATCH"]
    w1["addAuditFields<br/>prepended"] --> w2["userMws<br/>incl. withMeta"] --> w3["moduleEntitlement<br/>appended"] --> w4["handler"]
  end

  subgraph io["import-xls / export-xls only"]
    i1["safePost or safeGet"] --> i2["setImportAction<br/>or setExportAction"] --> i3["rbac full<br/>or rbac view"] --> i4["multer single file<br/>import only"] --> i5["handler"]
  end
```

`rbac()` is **not** in the default CRUD chain. Across the 68 entity routers, 13
reference `rbac(` at all: every router under `system/tenants/`, plus
`vendorsRouter`, `clientsRouter`, `employeesRouter`, `vendorContactsRouter`,
`arInvoicesRouter` (approve), `journalEntriesRouter`, `budgetsRouter`, and
`postingQueuesRouter`. Everywhere else — all of `projects`, `ap`, `bom`,
`reports`, and most of `core` and `accounting` — baseline CRUD is authenticated
and module-entitled, with layer-1 policy gating only on `/import-xls` and
`/export-xls`. Layers 2 through 4 still filter rows and columns, but only for
controllers that set `this.rbacConfig`.

`/ping` intentionally carries no middleware. Routers that must not leak their
existence pass `disablePing: true`.

---

## 5. Request trace

`GET /api/ar/v1/ar-invoices?project_id=…` with `Cookie: auth_token=…` and
`x-tenant-code: SRH`.

```mermaid
sequenceDiagram
  autonumber
  participant C as Client
  participant A as app.js
  participant AR as authRedis
  participant PG as Postgres
  participant RD as Redis
  participant AC as auditContext
  participant RT as Routers
  participant MW as route middleware
  participant VC as ViewController
  participant DB as db.js

  C->>A: GET /api/ar/v1/ar-invoices
  A->>A: cors, json, cookies, morgan
  A->>AR: not a bypass path
  AR->>AR: jwt.verify auth_token
  AR->>PG: admin.portal_users + portal_user_tenants
  AR->>PG: admin.tenants where tenant_code = srh
  Note over AR,PG: binding guard, schema resolves to srh
  AR->>RD: GET perm:uid:srh
  alt cache miss
    AR->>PG: loadPermissions from srh.*
    AR->>RD: primePermCache
  end
  AR->>AR: calcPermHash vs ph claim
  AR->>RD: GET imp:uid — none
  AR->>AC: req.user, req.ctx.schema = srh
  AC->>RT: requestContext.run — ALS scope
  RT->>RT: apiRoutes to arApiRoutes to arInvoicesRouter
  Note over RT: PUT /approve does not match,<br/>falls through to createRouter GET /
  RT->>MW: safeGet chain
  MW->>MW: req.resource = ar / ar-invoices
  MW->>MW: tenant allowed_modules includes ar
  MW->>VC: arInvoicesController.get
  VC->>VC: getSchema req to srh
  VC->>VC: _applyRbacFilters — layers 2, 3, 4
  VC->>DB: model srh
  DB->>PG: SELECT from srh.ar_invoices
  PG-->>VC: rows
  VC->>VC: _filterRecordFields — layer 4
  VC-->>C: res.json rows
```

On throw, `ViewController.handleError` maps the Postgres error code and usually
terminates the request itself — `23505` to 409, `23503` to 400, `22001` to 400.
The global [errorHandler](../../apps/server/src/middleware/errorHandler.js)
therefore fires rarely; when it does, it maps `SchemaDefinitionError` to 400,
`23505` to 409, `23503` to 422, passes through `err.status`, and otherwise
returns 500 with the message hidden in production.

The `auditContext` AsyncLocalStorage scope is what lets pg-schemata stamp
`created_by` / `updated_by` without any controller threading the actor through
explicitly. `addAuditFields`, despite its name, no longer sets those columns; it
guards mutation routes and injects `tenant_code` / `tenant_id` on POST.

---

## 6. Data layer

```mermaid
flowchart TD
  reg["db/moduleRegistry.js<br/>9 entries — name, scope,<br/>repositories, migrations"]
  reg --> repos["db/repositories.js<br/>flattens to one<br/>modelKey to ModelClass map"]
  repos --> init["DB.init DATABASE_URL,<br/>repositories, logger"]
  init --> raw["DB.db — pg-promise<br/>with models attached"]
  raw --> cdb["createCallDb in db.js"]

  cdb --> fn["Exported db is CALLABLE<br/>db vendors, srh<br/>to setSchemaName srh"]
  cdb --> props["Model properties<br/>db.vendors, db.arInvoices"]
  cdb --> pass["pg-promise passthroughs<br/>any, oneOrNone, tx, task, ..."]

  reg -.->|"scope: admin — auth only"| adm["Literal admin schema<br/>db portalUsers, admin"]
  reg -.->|"scope: tenant — 8 modules"| ten["dbSchema: tenantid placeholder<br/>bound per call"]
```

Only `auth` is `scope: 'admin'`. `core` and all seven business modules are
`scope: 'tenant'`.

### 6.1 Where the schema switch happens

Two places, and nothing uses `SET search_path`:

```mermaid
flowchart LR
  s1["1. authRedis<br/>reads admin.tenants.schema_name<br/>writes req.ctx.schema"]
  s2["2. ViewController.getSchema req<br/>ctx.schema, else user.schema_name,<br/>else user.tenant_code — lowercased"]
  s3["db model, schema<br/>to setSchemaName"]
  s4["Raw-SQL controllers<br/>pgp.as.name schema<br/>interpolation"]

  s1 --> s2 --> s3
  s2 --> s4
```

Every controller call re-reads the schema and rebinds the model. Reports and
other raw-SQL paths interpolate the schema name through `pgp.as.name` instead.

### 6.2 Tenant provisioning

```mermaid
flowchart TD
  cli["scripts/db/provisionTenantCli.js<br/>or CreateTenantWizard"] --> setup["services/tenantSetup.js<br/>provisionNewTenant"]
  setup --> ins["INSERT admin.tenants"]
  ins --> prov["services/tenantProvisioning.js<br/>CREATE SCHEMA name"]
  prov --> mig["db/migrations/createMigrator.js<br/>substitutes tenantid placeholder"]
  mig --> rec["Records applied ids in<br/>pgschemata.migrations<br/>schema_name, module_name,<br/>id, checksum"]
  rec --> seed["Seed RBAC catalog, self-company,<br/>admin employee + portal user"]
```

Schema arrives through pg-schemata `createTable` at tenant bootstrap — there are
no standalone data-definition-language (DDL) or backfill migration files.

---

## 7. Role-based access control

Four layers (ADR-0013, superseding ADR-0004). They execute in two different
places, and only the first is middleware.

```mermaid
flowchart TD
  subgraph l1["Layer 1 — Policies — middleware/rbac.js"]
    p1["resolveLevel caps, module, router, action"]
    p2["Cascade:<br/>module::router::action<br/>to module::router::<br/>to module::::<br/>to ::::"]
    p3["EXACT_MATCH_KEYS skip the cascade"]
    p4["Levels ascend: none, view, full<br/>no super_user bypass"]
    p1 --> p2 --> p3 --> p4
  end

  subgraph l234["Layers 2-4 — ViewController"]
    q1["buildQueryContext<br/>services/rbacQueryContext.js"]
    q2["Layer 2 — data scope<br/>pushes rbacConfig.scopeColumn IN ..."]
    q3["Layer 3 — state filters<br/>pushes status IN ..."]
    q4["Layer 4 — field groups<br/>narrows options.columnWhitelist<br/>+ _filterRecordFields on the way out"]
    q1 --> q2 --> q3 --> q4
  end

  l1 -->|"only on gated routes"| l234
```

Layer 1 gates only the routes that explicitly mount `rbac()` — see §4.1. Layers
2 through 4 run inside `ViewController` for any controller that sets
`this.rbacConfig`, independent of whether layer 1 ran.

Permission canons are assembled by
[permissionLoader.js](../../apps/server/src/services/permissionLoader.js) from
the tenant schema and cached in Redis for 900 seconds.
[permCacheInvalidator.js](../../apps/server/src/services/permCacheInvalidator.js)
evicts on role and policy change, fire-and-forget, with the TTL as the backstop.

---

## Client side, briefly

```mermaid
flowchart TD
  main["main.jsx"] --> tp["ThemeProvider — OS colour scheme"]
  tp --> cb["CssBaseline"]
  cb --> qc["QueryClientProvider<br/>staleTime 5 min, retry 1"]
  qc --> ap["AuthProvider<br/>also owns tenant + impersonation"]
  ap --> tpp["TenantPreferencesProvider"]
  tpp --> map["ModuleActionsProvider"]
  map --> app["App.jsx — BrowserRouter"]
  app --> shell["LayoutShell — the auth guard<br/>Navigate to /login if no user"]
  shell --> outlet["Outlet keyed on tenant_code<br/>remounts on tenant switch"]
```

Data access is three hand-written layers, no axios and no code generation:

```mermaid
flowchart LR
  hook["hooks/useVendors.js<br/>TanStack Query<br/>useVendors, useCreateVendor, ..."]
  svc["services/vendorApi.js<br/>BASE /core/v1/vendors<br/>list, create, update, archive"]
  cl["services/client.js<br/>fetch, credentials: include<br/>single-flight 401 refresh<br/>x-tenant-code injection<br/>getAll keyset cursor follow"]
  api["/api/*"]

  hook --> svc --> cl --> api
```

`BrowserRouter` lives in `App.jsx`, not `main.jsx`, so the router is inside
every provider. There is no `ProtectedRoute` component — `LayoutShell` is the
guard.
Route-level capability enforcement is absent by design: the sidebar filters
navigation by `user.perms.caps`, and the server is the actual gate.
